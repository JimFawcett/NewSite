// Hello - entry point for every Rust binary crate — the runtime calls main() to start execution.
fn main() {
    // println! is a macro (note the !); it writes a line to stdout.
    // String literals in Rust are UTF-8 encoded and have type &str.
    println!("Hello, world!");
}
